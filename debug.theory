chris
  theory main
    html is myff(5, this);
      div
        a
          span
        b

    ff myff(n)
      where start = n;
      ...
        -> { chris: twoChris }
           where twoChris = 2 * start
           yield start = 3 * twoChris;

    fn john(a, node) ->
      if (debug(len(node.children())) > 0) then
        john(a, node.apply({chris: 'was here'}, node.children()[0]))
      else {}
      endif;
