website
  import "test" into test;

  .: main
    div is a.block;
      #ident
        is { color: rgba(0.5, 0.75, 0.8, 0.5).sixToneCWScheme() }, alex(5,7), { 'font-size' : reduce(\x, a => x + a, [1,2,3,4], 0) };
        @bob is { 'font-size' : 12pt };
        chris > was
          is { here: "!" };
      child :: nother
      

    bob = 'width > 800px';

    fn alex(a,b) -> { 'background-color' : 'rgba(' + (a*b) + ',' + (b-a) + ',' + (b**a) + ')' };

    ff alex2(a,b)
      div
        -> style chris where chris = {};
        block
          -> style chris;

    ff makeColor(c)
      any
        -> style { 'background-color' : c };
        #header
        ...
          ...
        div 
      bob

  .: nother
    notherDiv
      withChild
        is { 'line-height' : 9px };
    

